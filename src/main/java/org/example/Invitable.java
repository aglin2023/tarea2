package org.example;

import java.util.ArrayList;
/**interfaz que define el comportamiento para invitar a empleados */
public interface Invitable {

    /**metodo que invita a una reunion
     * @param r la reunion a la que se invita */
    void Invitar(Reunion r);
}